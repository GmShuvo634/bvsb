*** Begin Patch
*** Update File: src/pages/play/index.tsx
@@
-      /**n       * Return the WS URL from env or default to localhost:5001n       */n      const buildUrl = () => process.env.NEXT_PUBLIC_WSS_URL || "ws://localhost:5001";n    const connect = () => {
+      /**
+       * Return the WS URL from env or default to localhost:5001
+       */
+      const buildUrl = () => {
+        return process.env.NEXT_PUBLIC_WSS_URL || "ws://localhost:5001";
+      };
+    const connect = () => {
*** End Patch
